MovieLens Dashboard
An interactive analytics dashboard built with Streamlit to visualize user behavior, genre trends, and movie recommendations.

Install Requirements
pip install streamlit pandas plotly numpy

Run the App Save the code as dashboard.py and run:
streamlit run dashboard.py
The dashboard will open in your browser automatically.
